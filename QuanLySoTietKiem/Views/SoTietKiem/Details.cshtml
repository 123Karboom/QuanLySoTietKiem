@{
  ViewData["Title"] = "Chi tiết sổ tiết kiệm";
  Layout = "~/Views/Shared/_UserLayout.cshtml";
}

<div class="container mt-5">
  <div class="alert alert-warning">
    <h4 class="alert-heading">Chức năng đang phát triển!</h4>
    <p>Vui lòng thử lại sau. 😊</p>
  </div>
  <a asp-action="Index" class="btn btn-primary">
    <i class="bi bi-arrow-left me-2"></i>Quay Lại Trang Danh Sách Sổ Tiết Kiệm  
  </a>
</div>
@* 

<div class="container mt-5">
  <div class="card shadow">
    <div class="card-header bg-info text-white">
      <div class="d-flex align-items-center">
        <i class="bi bi-info-circle-fill me-2"></i>
        <h3 class="mb-0">Chi Tiết Sổ Tiết Kiệm - @Model.Code</h3>
      </div>
    </div>
    <div class="card-body">
      <div class="row">
        <div class="col-md-6">
          <div class="mb-3">
            <label class="fw-bold">Khách hàng:</label>
            <p>@Model.TenKhachHang</p>
          </div>
          <div class="mb-3">
            <label class="fw-bold">Số Tiền Gửi Ban Đầu:</label>
            <p>@Model.SoTienGui.ToString("N0") VNĐ</p>
          </div>
          <div class="mb-3">
            <label class="fw-bold">Số Dư Hiện Tại:</label>
            <p class="text-success fw-bold">@Model.SoDuSoTietKiem.ToString("N0") VNĐ</p>
          </div>
          <div class="mb-3">
            <label class="fw-bold">Loại Sổ:</label>
            <p>@Model.TenLoaiSo (@Model.KyHan tháng)</p>
          </div>
        </div>
        <div class="col-md-6">
          <div class="mb-3">
            <label class="fw-bold">Hình Thức Đến Hạn:</label>
            <p>@Model.TenHinhThucDenHan</p>
          </div>
          <div class="mb-3">
            <label class="fw-bold">Lãi Suất Áp Dụng:</label>
            <p>@(Model.LaiSuatApDung * 100)%</p>
          </div>
          <div class="mb-3">
            <label class="fw-bold">Ngày Mở Sổ:</label>
            <p>@Model.NgayMoSo.ToString("dd/MM/yyyy")</p>
          </div>
          <div class="mb-3">
            <label class="fw-bold">Ngày Đáo Hạn:</label>
            <p>@Model.NgayMoSo.AddMonths(Model.KyHan).ToString("dd/MM/yyyy")</p>
          </div>
          <div class="mb-3">
            <label class="fw-bold">Trạng Thái:</label>
            @if (Model.TrangThai)
            {
              <span class="badge bg-success">Đang hoạt động</span>
            }
            else
            {
              <span class="badge bg-danger">Đã đóng</span>
            }
          </div>
        </div>
      </div>
    </div>
    <div class="card-footer">
      <div class="d-flex justify-content-between">
        <a asp-action="Index" class="btn btn-secondary">
          <i class="bi bi-arrow-left me-2"></i>Quay Lại
        </a>
        @if (Model.TrangThai)
        {
          <div>
            <a asp-action="Deposit" asp-route-id="@Model.MaSoTietKiem" class="btn btn-success me-2">
              <i class="bi bi-cash me-2"></i>Gửi Thêm
            </a>
            <a asp-action="Withdraw" asp-route-id="@Model.MaSoTietKiem" class="btn btn-warning">
              <i class="bi bi-cash-stack me-2"></i>Rút Tiền
            </a>
          </div>
        }
      </div>
    </div>
  </div>
</div> *@